QUESTION:

Locate the description of the `while` loop in the ruby documentation.

ANSWER:

As per the documentation in the "control expressions" section: The `while loop` executes while a condition
is `true`. When the condition results in `false` the loop is terminated.
